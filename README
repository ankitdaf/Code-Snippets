This repo is a backup of all my random and short code snippets and scripts that I find useful

androidcsvcleaner provides utilities to clean the messy and huge csv files that the Android phones generated when exporting to a VCF on an SDCard

Converter.py takes a VCF file generated by the Casio digital diaries, and converts them to readable contacts in plaintext

dupes.py eliminates duplicates , and uses the fdupes utility

NokiaConverter.py converts the VCF file generated by the Casio Digital Diaries into a format suitable to be imported by Nokia phones
